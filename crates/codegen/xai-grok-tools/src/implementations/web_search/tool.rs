//! Web search tool — old `impl Tool` deleted.
//! New implementation is in `grok_build/web_search/`.
//! Client logic is in `client.rs`; wire shapes in `backends/`; backend
//! selection in `factory.rs`.
